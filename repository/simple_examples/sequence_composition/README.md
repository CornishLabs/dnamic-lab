# Sequence-composition examples

`bound_stage_transition.py` demonstrates the current parts, lifecycle, and parameter
binding conventions using real tweezer hardware.

`atom_MW_state_change.py` is an older simulated example. It remains useful for seeing
how a one-shot fragment, repetition, and ndscan analysis can be assembled, but it
predates the current laboratory hardware architecture.
